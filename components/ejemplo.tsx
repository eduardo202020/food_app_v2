import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const ejemplo = (props: Props) => {
  return (
    <View>
      <Text>ejemplo</Text>
    </View>
  );
};

export default ejemplo;

const styles = StyleSheet.create({});
